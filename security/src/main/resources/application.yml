server:
 port: 8083
spring:
  profiles:
   active:
    - dev
  application:
    name: security

  datasource:
    url: jdbc:mysql://localhost:3306/jwt_securitydb
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

    
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
        
 
  kafka:
    template:
      default-topic: pro-topic1
    producer:
      bootstrap-servers: localhost:9092
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
     
#to generate new key run 'openssl rand -hex 32' in git bash 
secret:
 key: 641afd4eb2031d45396e27476a2bfb2df733d625323aba3dd2eba1460a58f85b

